git config --global user.name username
git config --global user.email useremail
git clone https url 
git add 
git commit 
git push 
git pull 
git cheakout
git branch
git branch -r
git status
git log
git init
git reset
git diff --staged ( for check the status at  staged area )
git diff --source
git mv 
git rm
git show (commit id or file name)
git branch (branch name)
git diff (when we are at source area)

